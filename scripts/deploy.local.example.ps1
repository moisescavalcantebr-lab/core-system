@{
    Server = "157.230.214.72"
    User = "root"
    RemoteZip = "/opt/workspace-update.zip"
    RemotePath = "/opt/workspace"
    # Use "no" para permitir senha no terminal; use "yes" para exigir chave SSH.
    BatchMode = "no"
}
